#include <stdio.h>
#include <string.h>
#include <math.h>

int main()
{
	int i, br, fakt = 1;

	do {
		printf("Upisite prirodni broj: ");
		scanf("%d", &br);
	} while (br <= 0);

	for (i = 1; i <= br; i++)
		fakt *= i;
	printf("Faktorijela broja %d je %d", br, fakt);
