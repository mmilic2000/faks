#include <stdio.h>

void obrni(char redak[], int i)
{
	if ((redak[i]) == '\0')
		return;
	else {
		obrni(redak, i + 1);
		printf("%c", redak[i]);
		return;
	}
}

int main()
{
	char redak[80];

	printf("Unesite redak teksta\n");
	fgets(redak, 80, stdin);
	obrni(redak, 0);
	return 0;
}
